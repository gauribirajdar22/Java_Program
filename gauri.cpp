#include <iostream>

// This line allows us to use standard library names like 'cout' directly
using namespace std;

int main() {
  // The 'cout' object is used with the insertion operator '<<' to print text
  cout << "Hello, World!"; 
  
  // 'return 0;' ends the main function and indicates the program ran successfully
  return 0;
}
